---
description: How to run any python script in this project.
alwaysApply: false
---

1. Use `uv` to run commands, e.g. `uv run $my_pyscript`.
2. Use `uv ci.py` (you can view the file if you need to) for CI such as lint & type check.
