---
description: Must-read to run anything python in the repo
alwaysApply: false
---
- We use Python 3.13. Don't use obsolete things. Use modern type hints.
- Use `uv` to run commands, e.g. `uv run $my_pyscript`.
- Use `uv ci.py` (you can view the file if you need to) for CI such as lint & type check.
